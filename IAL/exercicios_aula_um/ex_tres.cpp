#include<iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    float x, y, z;
    cout << "Escreva um número: ";
    cin >> x; 
    cout << "Escreva um número: ";
    cin >> y; 
    cout << "Escreva um número: ";
    cin >> z; 
    cout << "A soma desses números é: " << x + y + z << endl;
    return 0;
}
